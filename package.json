import React from 'react';
import { Link } from 'react-router-dom';
import { motion } from 'framer-motion';

const NotFound = () => (
  <div style={{ minHeight: '80vh', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', textAlign: 'center' }}>
    <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}>
      <div style={{ fontFamily: 'Orbitron', fontSize: '8rem', fontWeight: 900, background: 'linear-gradient(135deg, #00d4ff, #b026ff)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', lineHeight: 1, marginBottom: 16 }}>404</div>
      <h2 style={{ fontFamily: 'Orbitron', color: '#475569', marginBottom: 16, fontSize: '1.2rem' }}>PAGE NOT FOUND</h2>
      <p style={{ color: '#334155', marginBottom: 32 }}>The page you're looking for doesn't exist in this dimension.</p>
      <Link to="/"><motion.button whileHover={{ scale: 1.05 }} className="btn-primary">RETURN HOME</motion.button></Link>
    </motion.div>
  </div>
);

export default NotFound;
